import java.util.Scanner;

public class TotientFunction {
    
    // Function to calculate Euler's Totient Function φ(n)
    public static int phi(int n) {
        int result = n;
        for (int p = 2; p * p <= n; p++) {
            if (n % p == 0) {
                // If p is a divisor of n, it's a prime factor
                while (n % p == 0)
                    n /= p;
                result -= result / p;
            }
        }
        // If n is a prime number greater than 1
        if (n > 1)
            result -= result / n;
        return result;
    }

    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        
        int q = scanner.nextInt();
        int[] queries = new int[q];

        for (int i = 0; i < q; i++) {
            queries[i] = scanner.nextInt();
        }

        for (int i = 0; i < q; i++) {
            System.out.print(phi(queries[i]) + " ");
        }

        scanner.close();
    }
}
